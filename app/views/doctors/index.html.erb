<!-- <h1>Doctors</h1>
<% @doctors.all.each do |doctor| %>
  <p><%= link_to doctor.name, doctor_path(doctor) %> - <%= doctor.patients.count%></p>
<% end %> -->

<!-- <h1>Doctors</h1>
<ul>
<% @doctors.each do |doctor| %>
  <li><%= link_to doctor.name, doctor_path(doctor) %> - <%= pluralize(doctor.patients.count, 'Patient') %></li>
<% end %>
</ul> -->

<h1>Patients</h1>

<ul>
<% @patients.all.each do |patient| %>
  <li>Name: <%= link_to patient.name, patient_path(patient) %> Number of Appointments: <%= patient.appointments.count%></li>
<% end %>
</ul>
